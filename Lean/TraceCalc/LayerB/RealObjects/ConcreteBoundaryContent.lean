import TraceCalc.LayerC.RealObjects.ConcreteBoundaryContent

/-! Compatibility shim: the implementation moved to LayerC because it is a
derived concrete-boundary-content layer built on LayerB foundations. -/