import Boundary.LFunctionsRootedTreeFinal.Final.AnalyticMotives.TraceCorQ.Quotient.Setoid.Owner
import Boundary.LFunctionsRootedTreeFinal.Final.AnalyticMotives.TraceCorQ.Quotient.Relation.NamedCoherence.Owner

/-!
# Named-coherence quotient equalities

This file exposes quotient-class equalities generated by named coherence
relations.
-/

namespace Boundary
namespace LFunctions
namespace AnalyticMotives

/-- Fubini coherence support is zero in the trace-correspondence quotient. -/
theorem TraceCorQQuotient.sound_fubiniSupportZero
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQQuotient.ofCandidate
      (TraceCorQRelationGenerator.fubiniSupportCandidate
        source
        target
        support) =
      TraceCorQQuotient.ofCandidate
        TraceCorQQuotientCandidate.empty :=
  TraceCorQQuotient.sound
    (TraceCorQQuotientRelation.fubiniSupportZero
      source
      target
      support)

/-- Schedule-exchange coherence support is zero in the trace-correspondence quotient. -/
theorem TraceCorQQuotient.sound_scheduleExchangeSupportZero
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQQuotient.ofCandidate
      (TraceCorQRelationGenerator.scheduleExchangeSupportCandidate
        source
        target
        support) =
      TraceCorQQuotient.ofCandidate
        TraceCorQQuotientCandidate.empty :=
  TraceCorQQuotient.sound
    (TraceCorQQuotientRelation.scheduleExchangeSupportZero
      source
      target
      support)

/-- Residue-channel coherence support is zero in the trace-correspondence quotient. -/
theorem TraceCorQQuotient.sound_residueChannelSupportZero
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQQuotient.ofCandidate
      (TraceCorQRelationGenerator.residueChannelSupportCandidate
        source
        target
        support) =
      TraceCorQQuotient.ofCandidate
        TraceCorQQuotientCandidate.empty :=
  TraceCorQQuotient.sound
    (TraceCorQQuotientRelation.residueChannelSupportZero
      source
      target
      support)

/-- Stokes-residue coherence support is zero in the trace-correspondence quotient. -/
theorem TraceCorQQuotient.sound_stokesResidueSupportZero
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQQuotient.ofCandidate
      (TraceCorQRelationGenerator.stokesResidueSupportCandidate
        source
        target
        support) =
      TraceCorQQuotient.ofCandidate
        TraceCorQQuotientCandidate.empty :=
  TraceCorQQuotient.sound
    (TraceCorQQuotientRelation.stokesResidueSupportZero
      source
      target
      support)

/-- Refinement coherence support is zero in the trace-correspondence quotient. -/
theorem TraceCorQQuotient.sound_refinementSupportZero
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQQuotient.ofCandidate
      (TraceCorQRelationGenerator.refinementSupportCandidate
        source
        target
        support) =
      TraceCorQQuotient.ofCandidate
        TraceCorQQuotientCandidate.empty :=
  TraceCorQQuotient.sound
    (TraceCorQQuotientRelation.refinementSupportZero
      source
      target
      support)

/-- Associativity coherence support is zero in the trace-correspondence quotient. -/
theorem TraceCorQQuotient.sound_associativitySupportZero
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQQuotient.ofCandidate
      (TraceCorQRelationGenerator.associativitySupportCandidate
        source
        target
        support) =
      TraceCorQQuotient.ofCandidate
        TraceCorQQuotientCandidate.empty :=
  TraceCorQQuotient.sound
    (TraceCorQQuotientRelation.associativitySupportZero
      source
      target
      support)

/-- Left-identity coherence support is zero in the trace-correspondence quotient. -/
theorem TraceCorQQuotient.sound_leftIdentitySupportZero
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQQuotient.ofCandidate
      (TraceCorQRelationGenerator.leftIdentitySupportCandidate
        source
        target
        support) =
      TraceCorQQuotient.ofCandidate
        TraceCorQQuotientCandidate.empty :=
  TraceCorQQuotient.sound
    (TraceCorQQuotientRelation.leftIdentitySupportZero
      source
      target
      support)

/-- Right-identity coherence support is zero in the trace-correspondence quotient. -/
theorem TraceCorQQuotient.sound_rightIdentitySupportZero
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQQuotient.ofCandidate
      (TraceCorQRelationGenerator.rightIdentitySupportCandidate
        source
        target
        support) =
      TraceCorQQuotient.ofCandidate
        TraceCorQQuotientCandidate.empty :=
  TraceCorQQuotient.sound
    (TraceCorQQuotientRelation.rightIdentitySupportZero
      source
      target
      support)

end AnalyticMotives
end LFunctions
end Boundary
