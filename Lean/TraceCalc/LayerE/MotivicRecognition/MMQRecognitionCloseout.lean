import TraceCalc.LayerE.MotivicRecognition.Package9Proofs

/-!
Layer 12D canonical semantic import for the final `MM(Q)` recognition closeout
and dependency-DAG sealing route.

New code should import this module rather than `Package9Proofs`.
-/