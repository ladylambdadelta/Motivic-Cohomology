import Boundary.LFunctionsRootedTreeFinal.Final.AnalyticMotives.Motives.Comparison.EffectiveRealization.Yoneda.Summary.Owner
import Boundary.LFunctionsRootedTreeFinal.Final.AnalyticMotives.Motives.Comparison.EffectiveRealization.Carrier.Summary.Owner
import Boundary.LFunctionsRootedTreeFinal.Final.AnalyticMotives.Motives.Comparison.EffectiveRealization.BoundaryTarget.Summary.Owner

/-!
# Effective-realization comparison boundary

This file re-exports the proved endpoint data for the effective-realization
bridge: analytic trace-Yoneda representables on the source side, concrete
trace-object carrier inputs, parent Boundary effective motives, and the
smooth-scheme endpoint in the parent Boundary `DMgm` target and its analytic
comparison-target alias with parent Boundary formulas.  The source-side
Yoneda preimage formulas are re-exported here as the morphism-level data that
the effective-realization comparison preserves, together with their
underlying trace-hom projections and ambient-presheaf formulas.  The stable
source boundary is also re-exported here; it uses the same compact generators
and underlying trace objects as the Yoneda source, then applies the stable
comparison-source endpoint.  The stable-source preimage layer sends Yoneda
source representable maps to stable-source maps through their recovered compact
generator morphisms.
-/

namespace Boundary
namespace LFunctions
namespace AnalyticMotives

end AnalyticMotives
end LFunctions
end Boundary
