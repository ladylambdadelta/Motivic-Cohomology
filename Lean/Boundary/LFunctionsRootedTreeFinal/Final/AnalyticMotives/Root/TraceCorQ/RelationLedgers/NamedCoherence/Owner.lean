import Boundary.LFunctionsRootedTreeFinal.Final.AnalyticMotives.TraceCorQ.RelationLedgers.NamedCoherence.Owner

/-!
# Top-root named-coherence relation ledgers

This file exposes singleton relation ledgers generated by named coherence
relations under the top-level `AnalyticMotivesRoot` namespace.
-/

namespace Boundary
namespace LFunctions
namespace AnalyticMotives

/-- The top root exposes Fubini singleton relation ledgers. -/
theorem AnalyticMotivesRoot.traceCorQRelationLedger_fubini_eq_singleton
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQRelationLedger.fubini source target support =
      TraceCorQRelationLedger.singleton
        (TraceCorQRelationGenerator.fubini
          source
          target
          support) :=
  TraceCorQRelationLedger.fubini_eq_singleton
    source
    target
    support

/-- The top root exposes schedule-exchange singleton relation ledgers. -/
theorem AnalyticMotivesRoot.traceCorQRelationLedger_scheduleExchange_eq_singleton
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQRelationLedger.scheduleExchange source target support =
      TraceCorQRelationLedger.singleton
        (TraceCorQRelationGenerator.scheduleExchange
          source
          target
          support) :=
  TraceCorQRelationLedger.scheduleExchange_eq_singleton
    source
    target
    support

/-- The top root exposes residue-channel singleton relation ledgers. -/
theorem AnalyticMotivesRoot.traceCorQRelationLedger_residueChannel_eq_singleton
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQRelationLedger.residueChannel source target support =
      TraceCorQRelationLedger.singleton
        (TraceCorQRelationGenerator.residueChannel
          source
          target
          support) :=
  TraceCorQRelationLedger.residueChannel_eq_singleton
    source
    target
    support

/-- The top root exposes Stokes-residue singleton relation ledgers. -/
theorem AnalyticMotivesRoot.traceCorQRelationLedger_stokesResidue_eq_singleton
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQRelationLedger.stokesResidue source target support =
      TraceCorQRelationLedger.singleton
        (TraceCorQRelationGenerator.stokesResidue
          source
          target
          support) :=
  TraceCorQRelationLedger.stokesResidue_eq_singleton
    source
    target
    support

/-- The top root exposes refinement singleton relation ledgers. -/
theorem AnalyticMotivesRoot.traceCorQRelationLedger_refinement_eq_singleton
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQRelationLedger.refinement source target support =
      TraceCorQRelationLedger.singleton
        (TraceCorQRelationGenerator.refinement
          source
          target
          support) :=
  TraceCorQRelationLedger.refinement_eq_singleton
    source
    target
    support

/-- The top root exposes associativity singleton relation ledgers. -/
theorem AnalyticMotivesRoot.traceCorQRelationLedger_associativity_eq_singleton
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQRelationLedger.associativity source target support =
      TraceCorQRelationLedger.singleton
        (TraceCorQRelationGenerator.associativity
          source
          target
          support) :=
  TraceCorQRelationLedger.associativity_eq_singleton
    source
    target
    support

/-- The top root exposes left-identity singleton relation ledgers. -/
theorem AnalyticMotivesRoot.traceCorQRelationLedger_leftIdentity_eq_singleton
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQRelationLedger.leftIdentity source target support =
      TraceCorQRelationLedger.singleton
        (TraceCorQRelationGenerator.leftIdentity
          source
          target
          support) :=
  TraceCorQRelationLedger.leftIdentity_eq_singleton
    source
    target
    support

/-- The top root exposes right-identity singleton relation ledgers. -/
theorem AnalyticMotivesRoot.traceCorQRelationLedger_rightIdentity_eq_singleton
    (source target : TraceRewritePath)
    (support : TraceCorQFormalSum) :
    TraceCorQRelationLedger.rightIdentity source target support =
      TraceCorQRelationLedger.singleton
        (TraceCorQRelationGenerator.rightIdentity
          source
          target
          support) :=
  TraceCorQRelationLedger.rightIdentity_eq_singleton
    source
    target
    support

end AnalyticMotives
end LFunctions
end Boundary
