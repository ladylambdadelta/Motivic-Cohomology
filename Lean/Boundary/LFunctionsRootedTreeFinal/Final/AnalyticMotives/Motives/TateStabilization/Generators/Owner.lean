import Boundary.LFunctionsRootedTreeFinal.Final.AnalyticMotives.Motives.Presheaves.RewriteMaps.ByKind.Maps.Owner

/-!
# Tate-stabilization generator maps

This file owns the concrete representable-presheaf maps generated by analytic
weight-drop rewrites before Tate stabilization.
-/

namespace Boundary
namespace LFunctions
namespace AnalyticMotives

/-- A weight-drop map to be inverted by analytic Tate stabilization. -/
def TraceTateStabilizationGenerator.weightDropMap
    (source target : QTraceExpression) :
    TraceCorQPresheafHom
      (TraceCorQPresheaf.representable
        (TraceRewriteGenerator.weightDrop source target).sourceObject)
      (TraceCorQPresheaf.representable
        (TraceRewriteGenerator.weightDrop source target).targetObject) :=
  TraceRewriteGenerator.weightDropRepresentableMap source target

/-- The Tate weight-drop map is the by-kind weight-drop representable map. -/
theorem TraceTateStabilizationGenerator.weightDropMap_eq
    (source target : QTraceExpression) :
    TraceTateStabilizationGenerator.weightDropMap source target =
      TraceRewriteGenerator.weightDropRepresentableMap source target :=
  rfl

/-- The trace preimage of a Tate weight-drop map is the weight-drop trace hom. -/
theorem TraceTateStabilizationGenerator.weightDropMap_preimage
    (source target : QTraceExpression) :
    TraceCorQPresheaf.representablePreimage
        (TraceTateStabilizationGenerator.weightDropMap source target) =
      (TraceRewriteGenerator.weightDrop source target).traceHom :=
  TraceRewriteGenerator.representableMap_preimage
    (TraceRewriteGenerator.weightDrop source target)

end AnalyticMotives
end LFunctions
end Boundary
