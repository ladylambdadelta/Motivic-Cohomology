import TraceCalc.LayerC.RealObjects.ConcreteBoundaryPresentation

/-! Compatibility shim: the implementation moved to LayerC because it is a
derived boundary-presentation layer built on LayerB foundations. -/